<%= form_for(post_table) do |f| %>
  <% if post_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_table.errors.count, "error") %> prohibited this post_table from being saved:</h2>

      <ul>
      <% post_table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
